name: Label Test

# Generated by Achrobat/Achrobat-Label-Sync. Do not edit by hand: this file is overwritten on
# every distribution run. The check itself lives in the reusable workflow referenced
# below, so changes to it apply automatically without this file being redistributed.

on:
  pull_request_target:
    types:
      - opened
      - synchronize
      - reopened
      - labeled
      - unlabeled
      - ready_for_review

permissions:
  contents: read

jobs:
  label-test:
    permissions:
      contents: read
      checks: write
      issues: write
      pull-requests: write
      statuses: write
    uses: Achrobat/Achrobat-Label-Sync/.github/workflows/label-test.yml@master
    with:
      label_sync_repository: Achrobat/Achrobat-Label-Sync
      label_sync_ref: master
    secrets: inherit
