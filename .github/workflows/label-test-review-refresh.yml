name: Label Test Review Refresh

# Generated by Achrobat/Achrobat-Label-Sync. Do not edit by hand: this file is overwritten on
# every distribution run.

on:
  pull_request_review:
    types:
      - submitted
      - edited
      - dismissed

permissions:
  contents: read

jobs:
  refresh-label-test:
    name: Refresh Label Test
    permissions:
      actions: write
      contents: read
      checks: write
      pull-requests: read
    uses: Achrobat/Achrobat-Label-Sync/.github/workflows/refresh-label-test.yml@master
    with:
      label_sync_repository: Achrobat/Achrobat-Label-Sync
      label_sync_ref: master
    secrets: inherit
